services:
  backend:
    environment:
      NODE_ENV: production
    ports: !override
      - "3001:3000"

  ai-service:
    ports: !reset []
    expose:
      - "8000"

  frontend:
    ports: !override
      - "3000:3000"
