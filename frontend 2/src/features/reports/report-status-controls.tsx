"use client";
import { useState } from "react";
export function ReportStatusControls({ onSubmit }: { onSubmit: (status: string, reason: string | null) => void }) { const [status, setStatus] = useState("VERIFIED"); const [reason, setReason] = useState(""); return <section><label htmlFor="moderation-status">Moderation status</label><select id="moderation-status" value={status} onChange={(event) => setStatus(event.target.value)}><option value="VERIFIED">Verified</option><option value="DISPUTED">Disputed</option><option value="REJECTED">Rejected</option></select>{status === "DISPUTED" || status === "REJECTED" ? <input aria-label="Moderation reason" value={reason} onChange={(event) => setReason(event.target.value)} /> : null}<button type="button" onClick={() => onSubmit(status, reason || null)}>Save status</button></section>; }
