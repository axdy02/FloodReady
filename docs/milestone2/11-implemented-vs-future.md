# Implemented vs future

Implemented: authenticated main report form, private evidence photos, coordinates, immediate final-report persistence, in-process background Gemini image triage, Open-Meteo recent/current weather context, persisted validation score/outcome, owner-triggered AI retry, database persistence, My Reports, map markers, map reload persistence, API contracts, audit rows, and local Compose configuration. The older draft/human-review path remains available in `wireframe/` for comparison.

Future: moderation dashboard workflow, notification delivery, duplicate clustering, official agency feeds, retention jobs, object storage, queue-based asynchronous analysis, model evaluation datasets, monitoring dashboards, and production secret management.
